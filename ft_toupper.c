/*This function is a recreation of main one "toupper" it's convert character from lower to upper case*/

#include "libft.h"

int	ft_toupper(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		c -= 32;
	}
	return (c);
}
